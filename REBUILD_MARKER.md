# Invictus Performance Site — reconstrução 2026

Este branch é uma reconstrução total baseada exclusivamente nas telas aprovadas em setembro de 2026. Nenhum layout visual anterior deve ser reaproveitado.

Rotas-alvo: `/`, `/campeonatos`, `/campeonatos/cardio`, `/entre-amigos`, `/power-lift`, `/drops`, `/conta`, `/admin`.

Assets visuais oficiais são organizados em `public/assets/invictus/` e não devem ser substituídos por imagens aproximadas.

`/admin` é uma exceção visual deliberada: não usa banner/hero promocional. É uma ferramenta operacional autenticada que controla dados reais do ecossistema e chama o backend administrativo do aplicativo.
