# Invictus Performance Site — reconstrução 2026

Este branch é uma reconstrução total baseada exclusivamente nas telas aprovadas em setembro de 2026. Nenhum layout visual anterior deve ser reaproveitado.

Rotas-alvo: `/`, `/campeonatos`, `/campeonatos/cardio`, `/entre-amigos`, `/power-lift`, `/drops`, `/conta`, `/admin`.

Assets visuais oficiais serão organizados em `public/assets/invictus/` e não devem ser substituídos por imagens aproximadas.